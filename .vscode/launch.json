{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Run fine_tuned_bert_for_movie_review_classification",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/src/fine_tuned_bert_for_movie_review_classification.py",
            "console": "integratedTerminal",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/src"
            }
        }
    ]
}